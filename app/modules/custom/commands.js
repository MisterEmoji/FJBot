/*
Custom stuff to do:
- cmds,
- action rows (with buttons, labels, etc.[these are components]),
- embeds
*/

module.exports = function execute(interaction, executionFlow) {
  console.log(interaction.options.data);
};
